import { Layout } from '../../layout/Layout';
import GoogleForm from '../../components/GoogleForm';

const Home = () => {
  return (
    <Layout>
      <GoogleForm />
    </Layout>
  );
};

export default Home;
